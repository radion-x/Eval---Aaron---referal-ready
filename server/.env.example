# Environment variables for Spine IQ Server

# Port for the backend server to run on
# Make sure this matches the target port in client/vite.config.js proxy and Nginx proxy_pass
SERVER_PORT=

# Your Claude API Key - KEEP THIS SECRET AND NEVER COMMIT .env FILE TO GIT
CLAUDE_API_KEY=""

# Optional: MongoDB Connection String (if you add database integration later)
MONGODB_URI="mongodb://localhost:27017/spineiq_db"

# Mailgun SMTP Configuration
MAILGUN_SMTP_SERVER=""
MAILGUN_SMTP_PORT=""
MAILGUN_SMTP_LOGIN=""
MAILGUN_SMTP_PASSWORD=""
EMAIL_SENDER_ADDRESS=""
EMAIL_RECIPIENT_ADDRESS=""
BCC_EMAIL_RECIPIENT_ADDRESS=""

# Dashboard Password
DASHBOARD_PASSWORD=""

# Session Secret (for express-session)
SESSION_SECRET=""

# Server Base URL (for constructing public links to uploaded files)
# Ensure this is the correct public-facing URL of your backend server
SERVER_BASE_URL="http://localhost:3000"





# Environment variables for Spine IQ Server

# Port for the backend server to run on
# Make sure this matches the target port in client/vite.config.js proxy and Nginx proxy_pass
SERVER_PORT=3713

# Your Claude API Key - KEEP THIS SECRET AND NEVER COMMIT .env FILE TO GIT
CLAUDE_API_KEY=""

# Optional: MongoDB Connection String (if you add database integration later)
MONGODB_URI="mongodb://localhost:27017/spineiq_db"

# Mailgun SMTP Configuration
MAILGUN_SMTP_SERVER="smtp.mailgun.org"
MAILGUN_SMTP_PORT="587"
MAILGUN_SMTP_LOGIN="admin@mg.aaronbuckland.com"
MAILGUN_SMTP_PASSWORD="Gv7!pXe@29Lq#kM"
EMAIL_SENDER_ADDRESS="admin@mg.aaronbuckland.com"
EMAIL_RECIPIENT_ADDRESS="admin.buckland@mog.com.au"
BCC_EMAIL_RECIPIENT_ADDRESS="abuckland@mog.com.au, mark.bingemann@gmail.com, radsokolov@gmail.com"

# Dashboard Password
DASHBOARD_PASSWORD="Gumtr33s22!"

# Session Secret (for express-session)
SESSION_SECRET="aVerySecureSessionSecretKey12345!@#$%"

# Server Base URL (for constructing public links to uploaded files)
# Ensure this is the correct public-facing URL of your backend server
SERVER_BASE_URL="eval.aaronbuckland.com"
